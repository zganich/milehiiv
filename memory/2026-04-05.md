# 2026-04-05

- Stripe CLI is authenticated for the `careerswarm` account.
- Local test checkout is verified end to end in test mode with Stripe CLI forwarding webhooks to the app.
- The local webhook listener returned `200` for `checkout.session.completed` and `invoice.paid`.
- Preference captured: if a legacy feature path is costly to unwind, it is okay to start fresh when that is the faster, cleaner, and more token-efficient route.
- Production Vercel env vars for Stripe are still unset.
- Live Stripe monthly price ID identified during the session: `price_1T7ihbRGBj1peK9YyLjUOqtZ` for the Pro Monthly plan.
- Production Vercel Stripe env vars are now set, including the live monthly price ID and a newly created live one-time report price ID: `price_1TIpRKRGBj1peK9Y0EmqwiEm`.
- A trailing newline in `NEXT_PUBLIC_APP_URL` caused Stripe checkout to reject the `success_url`; correcting that env var fixed the live checkout redirect.
